# list properties
 indexed
 heterogeneous- mixed data types
 ordered
 can hold duplicate elements 
 represented by `[ ]`
 list() function converts other sequence into list
 mutable - it can change value during execution


 # what u need to learn?
 creating list 
 indexing and slicing
 looping/ traversal of list
 functions of list comprehension
 1)adding item
 -append()  # adds value at the end of list
 -insert
 -extend
 2) removing item
 -remove
 -clear
 -pop
 3)utility
 -reverse
 -sort
 -count
 -index
 -copy
 list comprehension
 lambda expression
 `map` and `filter`